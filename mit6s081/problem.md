#Trap.c:213中为什么scause == 0x80000001代表timer interrupt.(参考riscv-privileged page71)
# 锁与中断之间的关系
为什么acquire的时候，需要关闭中断, 而release的时候，又需要打开中断

# sys\_dup中需要增加一个文件的ref, 为什么不是增加一个inode的ref, file的ref和inode的ref区别在哪？
